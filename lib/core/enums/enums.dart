enum TypeOfDevice {
  bigDevice,
  smallDevice,
  normalDevice,
  mediumSmallDevice,
}

enum OptionalMessageSelect {
  deleteMessage,
  editMessage,
}

enum StatusTypingMessage {
  typing,
  renderReply,
  none,
}
